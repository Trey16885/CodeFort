/* ------------------------------------------------------------------
 * config.generated.js — PLACEHOLDER
 *
 * This file is regenerated by .github/workflows/deploy.yml on every
 * deploy, which fills it from the repository secrets:
 *
 *   KEY_TOKEN -> mistralKey      (Mistral API key)
 *   SUP_URL   -> supabaseUrl     (Supabase project URL)
 *   SUP_PB    -> supabaseKey     (Supabase publishable / anon key)
 *
 * The committed copy is intentionally empty so local checkouts and
 * forks run key-less; users then supply their own key in Settings.
 * Anything baked in here ships to every visitor — see docs/SECURITY.md.
 * ------------------------------------------------------------------ */
window.__CODEFORT_CONFIG__ = {
  mistralKey: "",
  supabaseUrl: "",
  supabaseKey: "",
  builtAt: "",
  commit: ""
};
